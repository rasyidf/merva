import { APP_NAME, APP_NAME_SHORT, APP_URL, APP_VERSION } from "@/shared/utils/constants";


export default {
  appName: APP_NAME,
  appShortName: APP_NAME_SHORT,
  appUrl: APP_URL,
  appVersion: APP_VERSION,
  defaultLocale: "id",
};